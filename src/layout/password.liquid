<!doctype html>
<html class="no-js supports-no-cookies" lang="{{ shop.locale }}">
    <head>
      {% include 'head' %}
      {{ content_for_header }}
    </head>

    <body id="{{ page_title | handle }}" class="template-{{ template.name | handle }}">
      <main id="content-for-layout">
        {{ content_for_layout }}
      </main>
    </body>
</html>
